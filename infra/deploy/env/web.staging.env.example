# Placeholder — copy to apps/web/.env.production on the STAGING checkout.
# NEXT_PUBLIC_* are baked in at `next build` time.

NEXT_PUBLIC_API_URL=https://STAGING_API_HOST/api/v1
NEXT_PUBLIC_WS_URL=wss://STAGING_API_HOST
API_PROXY_URL=http://127.0.0.1:4020

# Must match apps/api/.env JWT_ACCESS_SECRET
JWT_ACCESS_SECRET=CHANGE_ME_same_as_api_jwt_access_secret
